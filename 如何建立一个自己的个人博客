---
title: 从零搭建个人博客：新手避坑全流程指南
date: 2026-08-02 18:00:00
tags:
  - 博客搭建
  - Hexo
  - 新手教程
categories: 技术杂谈
---

> 本篇指南结合了真实的踩坑经历整理而成。从环境配置到上网部署，专门整理了新手最容易遇到的五大“拦路虎”及解决方法，希望能帮你节省大量时间。

## 一、 准备工作与核心逻辑

搭建一个独立博客并不难，你只需要本地写好代码，然后通过 <abbr title="一种用于代码版本控制的工具，可以帮你记录修改历史，并同步到GitHub。">Git</abbr> 发送到 <abbr title="GitHub提供的免费静态网页托管服务，能将你的代码仓库直接变成一个网站。">GitHub Pages</abbr> 上即可。在这之前，我们需要准备三个基础软件：

1. **安装 <abbr title="一个基于Chrome V8引擎的JavaScript运行环境，许多现代前端开发工具（包括Hexo）都依赖它运行。">Node.js</abbr>（务必选择 LTS 长期支持版本）**：这是运行博客框架的核心。
2. **安装 <abbr title="开源的版本控制系统，用于记录代码的变更历史，方便与GitHub进行交互。">Git</abbr>**：用来把本地代码推送到网上。
3. **安装 <abbr title="由微软开发的强大代码编辑器，内置终端控制台，非常适合编写代码和博客。">VS Code</abbr>**：你将在这里写博客的源代码，并执行后面的命令行操作。

## 二、 本地博客搭建（Hexo 初始化）

在电脑上随便新建一个文件夹（如 `D:\my-blog`），**鼠标右键点击该文件夹，选择“用 VS Code 打开”**。在 VS Code 的“终端”菜单中新建一个终端，执行以下命令：

```powershell
npm install hexo-cli -g
hexo init my-blog
cd my-blog
npm install
hexo server
验证：打开浏览器访问 http://localhost:4000。如果你看到默认的 Hexo 初始页面，说明本地博客已经成功运行！（按 Ctrl + C 可停止本地预览服务）。

## 三、配置 GitHub 与线上部署

创建 GitHub 仓库：登录 GitHub，点击右上角“+”号，新建仓库。名字必须严格命名为：你的GitHub用户名.github.io。直接点击创建即可，不要勾选任何选项。

配置 <abbr title="安全外壳协议，用于在电脑和GitHub服务器之间建立安全的加密连接通道，免去每次输入密码的麻烦。">SSH</abbr> 钥匙：

1. 在 VS Code 终端中输入：

\`\`\`powershell
ssh-keygen -t ed25519 -C "你的GitHub邮箱@xxx.com"
\`\`\`

连续按三次回车即可完成默认公私钥对生成。

2. 复制公钥：

\`\`\`powershell
cat ~/.ssh/id_ed25519.pub
\`\`\`

将终端中打印的公钥全部复制。

3. 回到 GitHub 网页，点击头像 -> Settings -> SSH and GPG keys -> New SSH key，将复制的公钥粘贴进去并保存。

修改 Hexo 配置：在 VS Code 里打开 `_config.yml`，滚动到最底部，修改为以下内容（注意冒号后要有空格）：

\`\`\`yaml
deploy:
  type: git
  repository: git@github.com:你的GitHub用户名/你的GitHub用户名.github.io.git
  branch: main
\`\`\`

安装部署插件：在终端执行：

\`\`\`bash
npm install hexo-deployer-git --save
\`\`\`

发布博客：依次执行以下命令：

\`\`\`powershell
hexo clean
hexo g
hexo d
\`\`\`

稍后访问 `https://你的GitHub用户名.github.io` 就能看到你的博客上线了。

## 四、新手必遇的 5 大报错及终极解法

在实际操作中，上述流程几乎不可能一次顺利跑通。以下是我亲身经历、详细验证的报错及解决方法：

### 💡 报错 1：终端提示 EPERM 权限不足

场景：输入任何包含 `npm` 的命令时，终端爆出大段红字，提示 `EPERM: operation not permitted`。

原因：Windows 系统的安全策略限制了 VS Code 普通权限去读取缓存文件。

解决方法：彻底关闭 VS Code。然后右键点击 VS Code 图标，选择“以管理员身份运行”，再次打开你的项目文件夹执行命令，报错就会消失。

### 💡 报错 2：测试 SSH 时提示 Connection refused

场景：测试连通性输入 `ssh -T git@github.com` 时，终端提示 `connect to host github.com port 22: Connection refused`。

原因：国内网络环境经常会屏蔽 GitHub 的默认 `22` 端口，导致无法连接。

解决方法：在终端中执行 `code ~/.ssh/config` 打开配置文件（如果没有会自动创建）。将以下配置粘贴进去并保存：

\`\`\`text
Host github.com
    HostName ssh.github.com
    User git
    Port 443
\`\`\`

再次测试，即可成功连通。

### 💡 报错 3：执行 hexo d 提示 failed to push some refs

场景：推送命令失败，提示分支不一致或文件冲突。

原因：GitHub 新建仓库的默认分支是 `main`，但本地 Hexo 生成的历史分支可能是 `master`。

解决方法：如果你想继续使用本地 `master` 分支，可执行：

\`\`\`powershell
git push -u origin master -f
\`\`\`

如果希望同步到 `main`，请先在本地创建 `main` 分支并推送。

### 💡 报错 4：GitHub 的 Actions 标签页出现红叉

场景：网页上线后乱码，或在 GitHub 仓库的 Actions 里看到报错日志，提示找不到 `theme landscape`。

原因：仓库根目录里残留了 `_config.yml`，GitHub 误以为你要使用 Jekyll 编译网站，但找不到该主题。

解决方法：在 GitHub 网页仓库中，点击 `Add file` -> `Create new file`，将文件命名为 `.nojekyll`（注意最前面有一个英文点），内容保持为空，直接提交。这会告诉 GitHub“不需要你编译，直接使用我本地生成好的网页”。

### 💡 报错 5：网页显示原始 Markdown 代码，没有排版

场景：打开博客主页（`https://xxx.github.io`），看到的却是一堆带有 `##`、反引号和 `>` 符号的纯文本。

原因：把 `source/_posts/` 里的 `.md` 源文件直接推到了 GitHub 仓库。浏览器无法解析原始 Markdown 文件，只会把它当成纯文本显示。

解决方法：

1. 在本地终端执行：

\`\`\`bash
hexo clean && hexo g
\`\`\`

2. 打开本地 `public/` 文件夹，确认其中包含 `index.html`、`css`、`js` 等静态资源。

3. 将 `public/` 的内容上传到 GitHub 仓库根目录，覆盖原来的错误文件。

## 五、日常发文章指南

经过上述流程，恭喜你已经拥有了一个自己的独立博客！以后每次发布新文章，只需这三步：

1. 在 VS Code 终端输入：

\`\`\`bash
hexo new "我的新文章标题"
\`\`\`

这会自动在 `source/_posts/` 下生成一个 `.md` 文件。

2. 用标准的 Markdown 语法写好文章，按 `Ctrl + S` 保存。

3. 在终端依次执行：

\`\`\`bash
hexo clean
hexo g
hexo d
\`\`\`

等待几分钟后，访问你的博客主页（建议按 `Ctrl + F5` 强制刷新），新文章就会顺利上线。

## 写在最后

搭建环境通常被看作是学编程的第一只“拦路虎”。它考验的其实不是写代码的能力，而是面对未知报错的耐心和搜索排查的能力。我希望这篇基于真实踩坑整理的文章，能成为你迈过这道门槛的阶梯。别怕报错，每次红字都在给你积累经验。祝你的博客之旅一切顺利！
'@; Set-Content -Path 'd:\B my-blog\source\_posts\如何建立一个自己的个人博客' -Value $h -Encoding utf8